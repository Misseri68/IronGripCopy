<style>
    :root {
        --primary-color: #FC5B04;
        --accent-color: orange;
        --background-color: #2c2c2c;
        --card-background: #0c0c0d;
        --text-color: white;
        --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        --border-radius: 8px;
        --blue-color: #205c77;
    }

    .exercise-form-container {
        background-color: var(--card-background);
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        padding: 16px;
        margin: 10px auto;
        max-width: 92%;
        border-left: 3px solid var(--primary-color);
    }

    .form-title {
        color: var(--primary-color);
        font-size: 18px;
        font-weight: 600;
        text-align: center;
        margin-bottom: 16px;
        padding-bottom: 8px;
        border-bottom: 1px solid #333;
    }

    .form-group {
        margin-bottom: 14px;
    }

    .exercise-form-container input[type="text"] {
        width: 100%;
        padding: 10px 12px;
        border: 1px solid #444;
        border-radius: var(--border-radius);
        font-size: 14px;
        outline: none;
        transition: all 0.3s ease;
        background-color: rgba(51, 51, 51, 0.6);
        color: var(--text-color);
        backdrop-filter: blur(5px);
    }

        .exercise-form-container input[type="text"]::placeholder {
            color: #999;
            font-size: 13px;
        }

        .exercise-form-container input[type="text"]:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 2px rgba(252, 91, 4, 0.2);
            background-color: rgba(51, 51, 51, 0.8);
        }

    .checkbox-wrapper {
        display: flex;
        align-items: center;
        margin: 12px 0;
        cursor: pointer;
    }

        .checkbox-wrapper input[type="checkbox"] {
            position: absolute;
            opacity: 0;
            cursor: pointer;
        }

    .custom-checkbox {
        position: relative;
        height: 20px;
        width: 20px;
        background-color: #333;
        border-radius: 4px;
        margin-right: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .checkbox-wrapper:hover .custom-checkbox {
        background-color: #444;
    }

    .checkbox-wrapper input:checked ~ .custom-checkbox {
        background-color: var(--primary-color);
    }

    .custom-checkbox:after {
        content: "";
        position: absolute;
        display: none;
        width: 6px;
        height: 10px;
        border: solid white;
        border-width: 0 2px 2px 0;
        transform: rotate(45deg);
    }

    .checkbox-wrapper input:checked ~ .custom-checkbox:after {
        display: block;
    }

    .checkbox-label {
        color: var(--text-color);
        font-size: 14px;
    }

    .file-input-wrapper {
        position: relative;
        margin: 14px 0;
    }

    .file-input-label {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        padding: 10px;
        background-color: #333;
        color: #ccc;
        border-radius: var(--border-radius);
        cursor: pointer;
        transition: all 0.3s;
        border: 1px dashed #555;
    }

    .file-input-wrapper input[type="file"] {
        opacity: 0;
        position: absolute;
        left: 0;
        width: 100%;
        height: 100%;
        cursor: pointer;
    }

    .file-input-label:hover {
        background-color: #3a3a3a;
        border-color: var(--primary-color);
    }

    .file-icon {
        margin-right: 8px;
        font-size: 16px;
    }

    .select-wrapper {
        position: relative;
        margin-bottom: 14px;
    }

    .exercise-form-container select {
        width: 100%;
        padding: 10px 12px;
        appearance: none;
        border: 1px solid #444;
        border-radius: var(--border-radius);
        background-color: #333;
        color: #ccc;
        font-size: 14px;
        cursor: pointer;
    }

    .select-wrapper:after {
        content: '▼';
        font-size: 10px;
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        color: #999;
        pointer-events: none;
    }

    .exercise-form-container select:focus {
        border-color: var(--primary-color);
        outline: none;
    }

    .submit-btn {
        background: linear-gradient(45deg, var(--primary-color), var(--accent-color));
        color: white;
        border: none;
        border-radius: var(--border-radius);
        padding: 12px;
        font-size: 15px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s;
        width: 100%;
        margin-top: 6px;
        text-transform: uppercase;
        letter-spacing: 1px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
            background: linear-gradient(45deg, var(--accent-color), var(--primary-color));
        }

        .submit-btn:active {
            transform: translateY(0);
        }
</style>

<div class="exercise-form-container mt-5">
    <h3 class="form-title">Nuevo Ejercicio</h3>
    <form method="post" enctype="multipart/form-data">
        <div class="form-group">
            <input type="text" name="nombre" placeholder="Nombre del ejercicio" class="form-control" />
        </div>

        <div class="form-group">
            <input type="text" name="descripcion" placeholder="Descripción breve" class="form-control" />
        </div>

        <label class="checkbox-wrapper">
            <input type="checkbox" name="esTiempo" />
            <div class="custom-checkbox"></div>
            <span class="checkbox-label">¿Basado en tiempo?</span>
        </label>

        <div class="file-input-wrapper">
            <input type="file" name="fichero" id="fichero" class="file-input" />
            <label for="fichero" class="file-input-label">
                <span class="file-icon">📷</span>
                Subir imagen del ejercicio
            </label>
        </div>

        <button type="submit" class="submit-btn">Crear Ejercicio</button>
    </form>
</div>